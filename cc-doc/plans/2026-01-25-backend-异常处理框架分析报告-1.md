# Backend 异常处理框架分析报告

> **更新日期**: 2026-01-25
> **状态**: 代码已重构，仅需优化重复定义

## 1. 现状评估

### 1.1 已完成的改进 ✅

代码已经进行了重大重构，主要问题已解决：

| 问题 | 状态 | 解决方案 |
|------|------|---------|
| 映射表维护繁琐 | ✅ 已解决 | 改用异常类属性 `http_status`/`error_code` |
| 响应格式不一致 | ✅ 已解决 | 统一为 `{"error": {"code", "message", "details", "trace_id"}}` |
| 未映射异常问题 | ✅ 已解决 | 不再需要映射表 |

### 1.2 仍存在的问题

**Auth 与 Security 异常重复定义**（11 个异常）：

| 异常类 | shared/security | auth/domain |
|--------|----------------|-------------|
| `UserNotFoundError` | ✓ | ✓ |
| `InvalidCredentialsError` | ✓ | ✓ |
| `AccountLockedError` | ✓ | ✓ |
| `TokenExpiredError` | ✓ | ✓ |
| ... 等 11 个 | | |

---

## 2. 确定的实施计划

### 决策确认 ✅

| 决策项 | 选择 |
|--------|------|
| 异常合并 | 改为重导出方式 |
| RFC 兼容 | 暂不需要（当前格式已足够） |

### 实施步骤

#### Step 1: 统一异常接口

**问题**：两处异常的构造函数签名略有不同。

| 异常类 | shared/security 签名 | auth/domain 签名 |
|--------|---------------------|------------------|
| `UserNotFoundError` | `(user_id: str)` | `(identifier: str \| int)` |
| `AccountLockedError` | `(message, locked_until)` | `(locked_until)` |

**行动**：保留 shared/security 的签名，检查业务代码调用方式。

#### Step 2: 重构 auth/domain/exceptions.py

```python
# modules/auth/domain/exceptions.py (改造后)
"""Auth domain exceptions - 重导出 shared/security 异常."""

from src.shared.infrastructure.security.exceptions import (
    SecurityError,
    AuthenticationError,
    UserNotFoundError,
    InvalidCredentialsError,
    AccountLockedError,
    AccountValidationError,
    TokenExpiredError,
    InvalidTokenError,
    PasswordExpiredError,
    PasswordTooWeakError,
    PasswordHistoryViolationError,
    InsufficientPermissionsError,
    SSOError,
    SSODegradedModeError,
)

# Auth 模块独有异常
class AuthError(SecurityError):
    http_status = 401
    def __init__(self, message: str, code: str = "AUTH_ERROR"):
        super().__init__(message, code=code)

class AccountInactiveError(AuthError):
    http_status = 401
    def __init__(self, message: str = "Account is not active"):
        super().__init__(message, code="ACCOUNT_INACTIVE")

class TokenError(AuthError):
    http_status = 401
    def __init__(self, message: str = "Token error", code: str = "TOKEN_ERROR"):
        super().__init__(message, code=code)

__all__ = [
    # 重导出
    "SecurityError", "AuthenticationError", "UserNotFoundError",
    "InvalidCredentialsError", "AccountLockedError", "AccountValidationError",
    "TokenExpiredError", "InvalidTokenError", "PasswordExpiredError",
    "PasswordTooWeakError", "PasswordHistoryViolationError",
    "InsufficientPermissionsError", "SSOError", "SSODegradedModeError",
    # 独有
    "AuthError", "AccountInactiveError", "TokenError",
]
```

#### Step 3: 更新业务代码调用

**检查范围**：
- `src/modules/auth/application/services/*.py`

#### Step 4: 验证测试

```bash
pytest tests/unit/modules/auth/ -v
pytest tests/integration/ -v
pytest
```

---

## 3. 关键文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/modules/auth/domain/exceptions.py` | 重构 | 改为重导出 |
| `src/modules/auth/application/services/*.py` | 检查 | 确保调用兼容 |

---

## 4. 风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| 构造函数签名不兼容 | 中 | 检查所有调用点 |
| 测试用例失败 | 低 | 更新断言 |
| 向后兼容性 | 低 | 重导出保持 API 不变 |
