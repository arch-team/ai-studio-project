# 优化根目录 CLAUDE.md — 逐项执行计划

## Context

用户要求逐个问题处理根 CLAUDE.md 的优化，每个问题先说明依据、获得确认后再执行。

## 当前执行：问题 1 — 英文章节标题中文化

**用户已确认同意执行。**

### 操作内容

纯文本替换，不增减行数：

| 行号 | 原文 | 替换为 |
|------|------|--------|
| 3 | `This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.` | `本文件为 Claude Code 提供项目级开发指导。` |
| 5 | `## Response Language` | `## 响应语言` |
| 24 | `## Project Overview` | `## 项目概述` |
| 34 | `## Architecture` | `## 架构概览` |
| 48 | `## Key Development Principles` | `## 核心开发原则` |
| 80 | `## Terminology Standards` | `## 术语标准` |
| 98 | `## Key Documentation` | `## 文档导航` |

保留 `# CLAUDE.md` 不变（文件名标识）。

### 涉及文件

- `CLAUDE.md` — 7 处纯文本替换

### 验证

- 替换后行数不变（148行）
- Grep 确认无其他文件通过锚点引用这些章节标题
