# tasks.md 状态同步计划

> 目标：将 tasks.md 中 Phase 6 和 Phase 7 的任务状态与实际代码实现同步
> 文件路径：`specs/001-ai-training-platform/tasks.md`

---

## 背景

代码探索发现 tasks.md 中 Phase 6（成本分析）和 Phase 7（在线开发环境）的任务全部标记为 `[ ]`，但后端代码实际已存在。需要将已实现的任务更新为 `[x]`，保留真正未完成的任务为 `[ ]`。

---

## 更新依据

### Phase 6 证据来源

| 任务 | 证据 | 结论 |
|------|------|------|
| T069 | `billing/application/services/cost_calculator.py` 已存在（157 行，三维度成本计算） | ✅ 标记完成 |
| T069a | `billing/infrastructure/external/cost_explorer_client.py` 已存在 | ✅ 标记完成 |
| T069b | `billing/application/services/pricing_model.py` 已存在（出现在 git status M 列表） | ✅ 标记完成 |
| T070 | `billing/application/services/usage_aggregator.py` 已存在 | ✅ 标记完成 |
| T071 | `billing/api/endpoints.py` 已存在，含 resource-usage 端点 | ✅ 标记完成 |
| T072 | `billing/api/endpoints.py` 已存在，含 cost-analysis 端点 | ✅ 标记完成 |
| T074 | `frontend/src/features/reports/pages/ResourceUsageReportPage.tsx` 已存在（git status M） | ✅ 标记完成 |
| T075 | `frontend/src/features/reports/pages/CostAnalysisPage.tsx` 已存在（git status M） | ✅ 标记完成 |
| T077 | `frontend/src/features/reports/components/CostTrendChart.tsx` 已存在 | ✅ 标记完成 |
| T078 | `billing/application/services/report_export_service.py` 已存在 | ✅ 标记完成 |
| T069c | `backend/tests/test_cost_accuracy.py` **未被探索确认** | ❌ 保持待完成 |
| T073 | `shared/infrastructure/external/cloudwatch_client.py` **未被探索确认** | ❌ 保持待完成 |
| T076 | `frontend/src/shared/components/forms/DateRangePicker.tsx` **未被探索确认** | ❌ 保持待完成 |

### Phase 7 证据来源

| 任务 | 证据 | 结论 |
|------|------|------|
| T081 | `spaces/api/endpoints.py` 已存在，含 POST /ide/sessions 端点 | ✅ 标记完成 |
| T082 | `spaces/api/endpoints.py` 已存在，含 GET /ide/sessions 端点 | ✅ 标记完成 |
| T083 | `spaces/api/endpoints.py` 已存在，含 GET /ide/sessions/{id} 端点 | ✅ 标记完成 |
| T084 | `spaces/api/endpoints.py` 已存在，含 DELETE /ide/sessions/{id} 端点 | ✅ 标记完成 |
| T085 | spaces 模块完整但 `sagemaker_spaces_service.py` **未单独确认** | ❌ 保持待完成 |
| T085a | `sagemaker_lifecycle_service.py` **未被探索确认** | ❌ 保持待完成 |
| T085b | `sagemaker_metrics_service.py` **未被探索确认** | ❌ 保持待完成 |
| T085c | `tests/test_sagemaker_spaces_performance.py` **未被探索确认** | ❌ 保持待完成 |
| T086 | `sagemaker_image_service.py` **未被探索确认** | ❌ 保持待完成 |
| T087 | `frontend/src/features/spaces/pages/CreateSpacePage.tsx` 为空占位 `export {}` | ❌ 保持待完成 |
| T088 | `frontend/src/features/spaces/pages/SpaceListPage.tsx` 为空占位 `export {}` | ❌ 保持待完成 |
| T089 | `frontend/src/features/spaces/pages/IDEFrame.tsx` 为空占位 `export {}` | ❌ 保持待完成 |
| T090 | `sagemaker_sync_service.py` **未单独确认**（SpaceService 有基础 CRUD 不含定时同步） | ❌ 保持待完成 |

---

## 实施步骤

### 步骤 1：读取 tasks.md 当前内容

- 文件：`specs/001-ai-training-platform/tasks.md`
- 目的：获取精确行内容，避免 edit 失配

### 步骤 2：精确替换 Phase 6 中 10 个任务的 checkbox

将以下任务从 `- [ ]` 改为 `- [x]`（**仅改 checkbox，不改任务描述**）：

```
T069   → [ ] → [x]  成本计算引擎核心逻辑
T069a  → [ ] → [x]  AWS Cost Explorer 集成
T069b  → [ ] → [x]  训练成本定价模型
T070   → [ ] → [x]  资源使用聚合查询
T071   → [ ] → [x]  GET /reports/resource-usage 端点
T072   → [ ] → [x]  GET /reports/cost-analysis 端点
T074   → [ ] → [x]  资源使用报表页面
T075   → [ ] → [x]  成本分析仪表盘
T077   → [ ] → [x]  成本趋势图表组件
T078   → [ ] → [x]  报表导出功能
```

保持待完成：T069c、T073、T076

### 步骤 3：精确替换 Phase 7 中 4 个任务的 checkbox

将以下任务从 `- [ ]` 改为 `- [x]`：

```
T081   → [ ] → [x]  POST /ide/sessions 端点实现
T082   → [ ] → [x]  GET /ide/sessions 端点实现
T083   → [ ] → [x]  GET /ide/sessions/{id} 端点实现
T084   → [ ] → [x]  DELETE /ide/sessions/{id} 端点实现
```

保持待完成：T085、T085a、T085b、T085c、T086、T087、T088、T089、T090

### 步骤 4：更新 Phase 进度摘要（如 tasks.md 顶部有进度统计）

检查 tasks.md 是否有任务统计行，如有则相应更新数字。

---

## 预期更新结果

| Phase | 更新前完成数 | 更新后完成数 | 总任务数 |
|-------|------------|------------|---------|
| Phase 6 | 0 | 10 | 13 |
| Phase 7 | 2 (T079/T080 重复) | 6 (2 原有 + 4 新) | 15 |

---

## 关键文件

| 文件 | 操作 |
|------|------|
| `specs/001-ai-training-platform/tasks.md` | **修改** - 更新 14 个任务的 checkbox 状态 |

---

## 验证

更新完成后，通过以下方式验证：
```bash
grep -c '\[x\]' specs/001-ai-training-platform/tasks.md   # 检查已完成任务总数
grep -n 'T069\|T070\|T071\|T072\|T074\|T075\|T077\|T078' specs/001-ai-training-platform/tasks.md
grep -n 'T081\|T082\|T083\|T084' specs/001-ai-training-platform/tasks.md
```
