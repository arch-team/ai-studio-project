# FastAPI and web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Database
sqlalchemy[asyncio]>=2.0.35
aiomysql>=0.2.0
alembic>=1.14.0

# Data validation
pydantic[email]>=2.10.0
pydantic-settings>=2.5.0

# AWS SDK
boto3>=1.35.3
botocore>=1.35.3
aioboto3>=13.0.0  # 原生异步 S3 客户端

# HyperPod SDK (训练任务提交必需，缺失时 POST /training-jobs 返回 503)
# 3.9.0 起官方排除 kubernetes 36.0.0（in-cluster BearerToken 认证损坏）并放宽 pyyaml
sagemaker-hyperpod>=3.9.0
kubernetes>=36.0.1

# Authentication
passlib[bcrypt]>=1.7.4
authlib>=1.3.0
PyJWT>=2.8.0

# HTTP client
httpx>=0.28.0

# MLflow for experiment tracking (T037a)
mlflow>=2.10.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0

# 开发/测试依赖请参见 requirements-dev.txt
