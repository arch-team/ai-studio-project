# FastAPI and web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Database
sqlalchemy[asyncio]>=2.0.35
aiomysql>=0.2.0
alembic>=1.14.0

# Data validation
pydantic[email]>=2.10.0
pydantic-settings>=2.5.0

# AWS SDK
boto3>=1.35.3
botocore>=1.35.3
aioboto3>=13.0.0  # 原生异步 S3 客户端

# HyperPod SDK (训练任务提交必需，缺失时 POST /training-jobs 返回 503)
sagemaker-hyperpod>=3.8.0
# 钉死 33.1.0: 36.0.0 的 in-cluster BearerToken 认证损坏（authsettings 键不匹配，请求不带 Authorization 头）；
# 修复版 36.0.1+ 需要 pyyaml>=6.0.3，与 sagemaker-hyperpod 钉死的 pyyaml==6.0.2 冲突
kubernetes==33.1.0

# Authentication
passlib[bcrypt]>=1.7.4
authlib>=1.3.0
PyJWT>=2.8.0

# HTTP client
httpx>=0.28.0

# MLflow for experiment tracking (T037a)
mlflow>=2.10.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0

# 开发/测试依赖请参见 requirements-dev.txt
