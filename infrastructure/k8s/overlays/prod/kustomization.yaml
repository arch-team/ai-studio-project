# Kustomize Prod Overlay - 生产环境配置
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

commonLabels:
  environment: prod

patches:
  - path: patches/enable-ha.yaml
  - path: patches/strict-policy.yaml
