# Kustomize Dev Overlay - 开发环境配置
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

commonLabels:
  environment: dev

patches:
  - path: patches/reduce-replicas.yaml
  - path: patches/reduce-quotas.yaml
