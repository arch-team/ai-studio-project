# 前端服务 Service (ClusterIP)
# 供 ALB TargetGroupBinding 和集群内部服务发现使用
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: ai-platform
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
